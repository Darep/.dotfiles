alias nano="subl"
alias hosts="sudo nano /etc/hosts"

# Ruby
alias mm="middleman"
alias fs="foreman start"
alias be="bundle exec"

# Rails
alias rt="RAILS_ENV=test rake test"
alias mig="rake db:migrate"

# Heroku
alias h="heroku"
alias hp="git push heroku"
alias ho="heroku open"
alias hc="heroku run console"

# Work stuff
alias liveen='cap production deploy && terminal-notifier -message "cap deploy done" -title "Check it, yo."'

# Firefox OS stuff
export FFOS_PROJ="/Users/ajk/work/gaia"
export FIREFOX="/Applications/FirefoxNightly.app/Contents/MacOS/firefox-bin"

alias gaiaff-make="cd $FFOS_PROJ; rm -rfv profile-debug; DEBUG=1 make"
alias gaiaff="$FIREFOX -profile $FFOS_PROJ/profile-debug -no-remote"
